Place the collage assets (e.g. `collage1.jpeg` …) in this folder.
The homepage canvas background stitches together all images listed in `collageSources` inside `script.js`, so keep the filenames in sync with that array.
